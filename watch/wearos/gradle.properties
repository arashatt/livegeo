org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# A map to build in, for a deployment whose address stays (a named tunnel, the
# Worker): here, with -Plivegeo.server=…, or LIVEGEO_SERVER in CI. Empty, the
# watch asks for the map's name when it pairs, which is what a quick tunnel,
# whose address changes, needs.
livegeo.server=
